{
  "name": "colyseus-test-client",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "start": "yarn build:env-config && react-scripts start",
    "build": "yarn build:env-config && react-scripts build",
    "build:env-config": "bash ./bin/build_env_config.sh && mv ./env-config.js ./public"
  },
  "dependencies": {
    "colyseus.js": "^0.10.8",
    "react": "^16.9.0",
    "react-dom": "^16.9.0",
    "react-scripts": "^3.1.1"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
